# NULL: a static hub, served straight from the repository by GitHub Pages.
#
# Crawlers only read /robots.txt at an origin's root, which is where NULL is
# served from. The 404 page carries its own noindex tag as well, so it stays
# out of results even if this file is never fetched.

User-agent: *
Allow: /

Disallow: /404.html
