# Dev-only leftovers (not needed to build or deploy: see README)
node_modules
dist
tsconfig.tsbuildinfo
.cache

# Host/editor secrets: never publish (NULL needs no keys to run)
.env.local
.env*.local
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
